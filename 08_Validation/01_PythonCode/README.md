# Following the introduction and instruction to execute the scripts in sequence to perform validation simulation and analysis
1. "populateBaselineInputs" and "populatePosteriorInputs" will create tables and insert baseline, carlibrated model inputs into the tables in DB.

2. "populateXMLConstruction" is only executed when overwriting CitySim.xml files after executing "XML_...Updater_AMSmodels" script